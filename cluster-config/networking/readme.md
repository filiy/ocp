# Networking

Generally NodeNetworkConfigurationPolicy files will live here. These will configure network interfaces on the OpenShift nodes. The yamls reference interface names like `bond0` and `eth2`. You will need to ensure those exist first.
